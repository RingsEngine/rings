export type OutlineSetting = {
  enable: boolean;
  groupCount: number;
  outlinePixel: number;
  fadeOutlinePixel: number;
  strength: number;
  // 描边模式
  useAddMode: boolean;
  textureScale: number;
  debug: boolean;
};
