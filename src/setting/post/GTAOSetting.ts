export type GTAOSetting = {
  enable: boolean;
  maxDistance: number;
  maxPixel: number;
  darkFactor: number;
  rayMarchSegment: number;
  // 多次反弹
  multiBounce: boolean;
  usePosFloat32: boolean;
  blendColor: boolean;
  debug: boolean;
};
