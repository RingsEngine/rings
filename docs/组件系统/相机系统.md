# 相机系统

### Camera3D 相机组件

`Camera3D` 类表示 3D 场景中的相机组件，支持透视投影和正交投影，并提供视锥体裁剪、阴影计算、坐标转换等功能。

#### 属性说明

| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| **基础投影参数** |
| `fov` | number | 60 | 视野角度（度），仅用于透视投影 |
| `aspect` | number | 1 | 视口宽高比（宽度/高度） |
| `near` | number | 1 | 近裁剪面距离 |
| `far` | number | 5000 | 远裁剪面距离 |
| **正交投影参数** |
| `left` | number | -100 | 正交投影左边界 |
| `right` | number | 100 | 正交投影右边界 |
| `top` | number | 100 | 正交投影上边界 |
| `bottom` | number | -100 | 正交投影下边界 |
| `frustumSize` | number | 100 | 正交投影视锥体大小 |
| **相机设置** |
| `type` | CameraType | CameraType.perspective | 投影类型（perspective或ortho） |
| `viewPort` | Rect | new Rect() | 相机视口矩形 |
| `isShadowCamera` | boolean | false | 是否为阴影相机 |
| **级联阴影** |
| `enableCSM` | boolean | false | 是否启用级联阴影映射 |
| `csm` | FrustumCSM | null | 级联阴影映射实例 |

#### 方法说明

| 方法名 | 描述 |
|--------|------|
| `perspective(fov, aspect, near, far)` | 设置透视投影参数 |
| `ortho(frustumSize, near, far)` | 设置正交投影参数 |
| `orthoOffCenter(left, right, bottom, top, near, far)` | 设置正交投影边界 |
| `lookAt(pos, target, up)` | 使相机朝向指定目标点 |
| `worldToScreenPoint(point)` | 将世界坐标转换为屏幕坐标 |
| `screenPointToRay(x, y)` | 从屏幕坐标创建射线 |
| `screenPointToWorld(x, y, z)` | 将屏幕坐标转换为世界坐标 |

#### 使用示例

```typescript
import { Object3D, Scene3D, Camera3D, Vector3 } from '@rings/core';

// 创建场景
const scene = new Scene3D();

// 创建相机对象
const cameraObj = new Object3D();
const camera = cameraObj.addComponent(Camera3D);

// 设置透视投影
camera.perspective(60, window.innerWidth / window.innerHeight, 0.1, 1000);

// 设置相机位置
cameraObj.transform.position = new Vector3(0, 5, 10);
cameraObj.transform.lookAt(new Vector3(0, 0, 0));

// 添加到场景
scene.addChild(cameraObj);
```

#### 可运行示例（Camera3D · perspective / ortho / 屏幕射线）

### 完整示例

下方为仓库内自带的 **可运行 HTML**：

- **在文档页内嵌预览**（若当前文档站点支持内联 HTML，可直接看到运行效果；左侧为示例，右侧为代码，可切换）：

<div class="rings-quick-demo" style="margin: 1em 0; border: 1px solid #e2e8f0; border-radius: 8px; overflow: hidden;">
  <input type="radio" name="rings-camera-demo-tab" id="rings-camera-tab-preview" checked style="position: absolute; opacity: 0; pointer-events: none;">
  <input type="radio" name="rings-camera-demo-tab" id="rings-camera-tab-code" style="position: absolute; opacity: 0; pointer-events: none;">
  <style>
    .rings-quick-demo .rings-tab-bar { display: flex; border-bottom: 1px solid #e2e8f0; background: #f8fafc; }
    .rings-quick-demo .rings-tab-bar label { flex: 1; padding: 10px 16px; cursor: pointer; font-size: 14px; color: #64748b; text-align: center; margin: 0; }
    .rings-quick-demo #rings-camera-tab-preview:checked ~ .rings-tab-bar label[for="rings-camera-tab-preview"],
    .rings-quick-demo #rings-camera-tab-code:checked ~ .rings-tab-bar label[for="rings-camera-tab-code"] { font-weight: 600; color: #0f172a; }
    .rings-quick-demo .rings-demo-preview,
    .rings-quick-demo .rings-demo-code { display: none; height: 420px; }
    .rings-quick-demo #rings-camera-tab-preview:checked ~ .rings-demo-preview,
    .rings-quick-demo #rings-camera-tab-code:checked ~ .rings-demo-code { display: block; }
  </style>
  <div class="rings-tab-bar">
    <label for="rings-camera-tab-preview">示例</label>
    <label for="rings-camera-tab-code">代码</label>
  </div>
  <div class="rings-demo-preview" style="background: #fff;">
    <iframe src="examples/camera3d-component-demo.html" title="Rings Camera3D 演示" style="width: 100%; height: 100%; border: none; min-height: 420px;"></iframe>
  </div>
  <div class="rings-demo-code" style="overflow: auto; background: #1e293b;">
    <pre style="margin: 0; padding: 16px; font-size: 12px; line-height: 1.5; color: #e2e8f0; white-space: pre;"><code>&lt;!DOCTYPE html&gt;
&lt;html lang="zh-CN"&gt;
&lt;head&gt;
  &lt;meta charset="UTF-8" /&gt;
  &lt;meta name="viewport" content="width=device-width, initial-scale=1.0" /&gt;
  &lt;title&gt;Rings — Camera3D 演示（可运行示例）&lt;/title&gt;
  &lt;style&gt;
    html, body { margin: 0; height: 100%; width: 100%; overflow: hidden; }
    body { position: relative; font-family: system-ui, "Segoe UI", sans-serif; }
    #canvas { display: block; width: 100%; height: 100%; }
    .hud {
      position: absolute; left: 8px; top: 8px; right: 220px; pointer-events: none;
      font-size: 11px; line-height: 1.45; color: #e6edf3; text-shadow: 0 1px 3px #000;
      font-family: ui-monospace, monospace;
    }
    .controls {
      position: absolute; top: 0; right: 0; bottom: 0; width: 200px; padding: 10px;
      overflow: auto; background: rgba(15, 20, 25, 0.92); color: #e6edf3;
      font-size: 0.78rem; border-left: 1px solid #2d3a4d;
    }
    .controls h3 { margin: 0 0 8px; font-size: 0.8rem; color: #8b9cb3; font-weight: 600; }
    .row { margin-bottom: 10px; }
    .row label { display: block; margin-bottom: 3px; color: #8b9cb3; }
    .row input[type="range"] { width: 100%; }
    .row .val { float: right; color: #58a6ff; }
    .radio-row label { display: inline-flex; align-items: center; gap: 5px; margin-right: 8px; cursor: pointer; color: #e6edf3; }
    @media (max-width: 520px) {
      .hud { right: 8px; }
      .controls { width: 100%; height: auto; max-height: 45%; top: auto; left: 0; border-left: none; border-top: 1px solid #2d3a4d; }
    }
  &lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
  &lt;canvas id="canvas" style="width: 100%; height: 100%;"&gt;&lt;/canvas&gt;
  &lt;div class="hud" id="hud"&gt;&lt;/div&gt;
  &lt;aside class="controls" id="controls"&gt;
    &lt;h3&gt;投影&lt;/h3&gt;
    &lt;div class="row radio-row"&gt;
      &lt;label&gt;&lt;input type="radio" name="proj" value="perspective" checked /&gt; perspective()&lt;/label&gt;
      &lt;label&gt;&lt;input type="radio" name="proj" value="ortho" /&gt; ortho()&lt;/label&gt;
    &lt;/div&gt;
    &lt;div class="row" id="row-fov"&gt;
      &lt;label&gt;fov&lt;span class="val" id="fov-val"&gt;60&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="fov" min="25" max="100" value="60" step="1" /&gt;
    &lt;/div&gt;
    &lt;div class="row" id="row-frustum" style="display:none"&gt;
      &lt;label&gt;frustumSize&lt;span class="val" id="frustum-val"&gt;32&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="frustum" min="8" max="80" value="32" step="1" /&gt;
    &lt;/div&gt;
    &lt;div class="row"&gt;
      &lt;label&gt;near&lt;span class="val" id="near-val"&gt;0.1&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="near" min="1" max="50" value="1" step="1" /&gt;
    &lt;/div&gt;
    &lt;div class="row"&gt;
      &lt;label&gt;far&lt;span class="val" id="far-val"&gt;1000&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="far" min="100" max="3000" value="1000" step="50" /&gt;
    &lt;/div&gt;
    &lt;p style="margin:8px 0 0;color:#8b9cb3;line-height:1.45"&gt;near 滑块÷10；点击画布可看 ray / screenPointToWorld。拖拽旋转（HoverCameraController）。&lt;/p&gt;
  &lt;/aside&gt;

  &lt;script type="module"&gt;
    import {
      Engine3D,
      Scene3D,
      Object3D,
      Camera3D,
      View3D,
      Vector3,
      CameraType,
      LitMaterial,
      BoxGeometry,
      SphereGeometry,
      PlaneGeometry,
      MeshRenderer,
      AtmosphericComponent,
      DirectLight,
      HoverCameraController,
      Color,
    } from 'https://unpkg.com/@rings-webgpu/core@1.0.52/dist/rings.es.js';

    const BOX_CENTER = new Vector3(0, 0.5, 0);

    function formatVec3(v) {
      return `${v.x.toFixed(2)}, ${v.y.toFixed(2)}, ${v.z.toFixed(2)}`;
    }

    async function main() {
      const canvas = document.getElementById('canvas');
      const hud = document.getElementById('hud');
      if (!canvas) throw new Error('缺少 #canvas');

      try {
        await Engine3D.init({ canvasConfig: { canvas } });
      } catch (e) {
        document.body.innerHTML =
          '&lt;p style="padding:1rem;font-family:sans-serif"&gt;WebGPU 初始化失败，请使用 Chrome/Edge 最新版并在 localhost 或 HTTPS 下打开。&lt;/p&gt;';
        console.error(e);
        return;
      }

      const scene = new Scene3D();
      const sky = scene.addComponent(AtmosphericComponent);
      sky.sunY = 0.6;
      sky.sunZ = 0.8;

      const cameraObj = new Object3D();
      const camera = cameraObj.addComponent(Camera3D);

      const aspect = () =&gt;
        canvas.clientWidth / Math.max(canvas.clientHeight, 1);

      let projMode = 'perspective';
      let fov = 60;
      let frustumSize = 32;
      let near = 0.1;
      let far = 1000;

      function applyProjection() {
        const a = aspect();
        if (projMode === 'perspective') {
          camera.perspective(fov, a, near, far);
        } else {
          camera.ortho(frustumSize, near, far);
        }
      }

      applyProjection();
      const hover = cameraObj.addComponent(HoverCameraController);
      hover.setCamera(35, -25, 12);
      scene.addChild(cameraObj);

      const lightObj = new Object3D();
      const light = lightObj.addComponent(DirectLight);
      light.lightColor = new Color(1, 1, 1, 1);
      light.intensity = 1.2;
      light.castShadow = true;
      scene.addChild(lightObj);

      const ground = new Object3D();
      const groundR = ground.addComponent(MeshRenderer);
      groundR.geometry = new PlaneGeometry(10, 10);
      groundR.material = new LitMaterial();
      groundR.material.baseColor = new Color(0.3, 0.3, 0.3, 1);
      groundR.receiveShadow = true;
      scene.addChild(ground);

      const box = new Object3D();
      const boxR = box.addComponent(MeshRenderer);
      boxR.geometry = new BoxGeometry(1, 1, 1);
      boxR.material = new LitMaterial();
      boxR.material.baseColor = new Color(0.8, 0.2, 0.2, 1);
      boxR.castShadow = true;
      box.y = 0.5;
      scene.addChild(box);

      const sphere = new Object3D();
      const sphereR = sphere.addComponent(MeshRenderer);
      sphereR.geometry = new SphereGeometry(0.5, 32, 32);
      sphereR.material = new LitMaterial();
      sphereR.material.baseColor = new Color(0.2, 0.8, 0.2, 1);
      sphereR.castShadow = true;
      sphere.x = 2;
      sphere.y = 0.5;
      scene.addChild(sphere);

      const view = new View3D();
      view.scene = scene;
      view.camera = camera;
      Engine3D.startRenderView(view);

      const fovEl = document.getElementById('fov');
      const fovVal = document.getElementById('fov-val');
      const frustumEl = document.getElementById('frustum');
      const frustumVal = document.getElementById('frustum-val');
      const nearEl = document.getElementById('near');
      const nearVal = document.getElementById('near-val');
      const farEl = document.getElementById('far');
      const farVal = document.getElementById('far-val');
      const rowFov = document.getElementById('row-fov');
      const rowFrustum = document.getElementById('row-frustum');

      function readUi() {
        fov = Number(fovEl.value);
        frustumSize = Number(frustumEl.value);
        near = Number(nearEl.value) / 10;
        far = Number(farEl.value);
        fovVal.textContent = String(fov);
        frustumVal.textContent = String(frustumSize);
        nearVal.textContent = near.toFixed(1);
        farVal.textContent = String(far);
      }

      document.querySelectorAll('input[name="proj"]').forEach((r) =&gt; {
        r.addEventListener('change', () =&gt; {
          projMode = r.value;
          rowFov.style.display = projMode === 'perspective' ? '' : 'none';
          rowFrustum.style.display = projMode === 'ortho' ? '' : 'none';
          readUi();
          applyProjection();
        });
      });
      [fovEl, frustumEl, nearEl, farEl].forEach((el) =&gt; {
        el.addEventListener('input', () =&gt; {
          readUi();
          applyProjection();
        });
      });

      readUi();

      window.addEventListener('resize', () =&gt; {
        applyProjection();
      });

      let lastRayStr = '（点击画布）';
      canvas.addEventListener(
        'pointerdown',
        (e) =&gt; {
          if (e.target !== canvas) return;
          const x = e.offsetX;
          const y = e.offsetY;
          const ray = camera.screenPointToRay(x, y);
          const w0 = camera.screenPointToWorld(x, y, 0.01);
          const w1 = camera.screenPointToWorld(x, y, 0.5);
          lastRayStr =
            `ray.origin: ${formatVec3(ray.origin)}\n` +
            `ray.direction: ${formatVec3(ray.direction)}\n` +
            `screenPointToWorld(z=0.01): ${formatVec3(w0)}\n` +
            `screenPointToWorld(z=0.5): ${formatVec3(w1)}`;
        },
        { passive: true }
      );

      function tick() {
        const screen = camera.worldToScreenPoint(BOX_CENTER);
        const typeName =
          camera.type === CameraType.perspective
            ? 'perspective'
            : camera.type === CameraType.ortho
              ? 'ortho'
              : 'other';
        hud.innerHTML =
          `&lt;div&gt;&lt;b&gt;camera.type&lt;/b&gt; ${typeName}&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;fov / frustumSize&lt;/b&gt; ${projMode === 'perspective' ? fov : frustumSize}&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;near / far&lt;/b&gt; ${near.toFixed(2)} / ${far}&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;worldToScreenPoint(红盒中心)&lt;/b&gt;&lt;/div&gt;` +
          `&lt;div&gt;${formatVec3(screen)}&lt;/div&gt;` +
          `&lt;div style="margin-top:8px"&gt;&lt;b&gt;点击 — screenPointToRay / screenPointToWorld&lt;/b&gt;&lt;/div&gt;` +
          `&lt;div style="white-space:pre-wrap"&gt;${lastRayStr}&lt;/div&gt;`;
        requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }

    main();
  &lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
  </div>
</div>

---

### 相机控制器

#### 自由相机控制器 (FlyCameraController)

用于实现第一人称自由飞行相机，支持WASD键移动和鼠标控制视角。

**属性说明**

| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| `moveSpeed` | number | 2.0 | 移动速度 |
| `lookSpeed` | number | 0.1 | 视角转动速度 |

**使用示例**

```typescript
import { Object3D, Scene3D, Camera3D, FlyCameraController, Vector3 } from '@rings/core';

// 创建场景
const scene = new Scene3D();

// 创建相机对象
const cameraObj = new Object3D();
const camera = cameraObj.addComponent(Camera3D);
const controller = cameraObj.addComponent(FlyCameraController);

// 设置相机参数
camera.perspective(60, window.innerWidth / window.innerHeight, 0.1, 1000);
controller.moveSpeed = 5.0;

// 设置初始位置
cameraObj.transform.position = new Vector3(0, 5, 10);
scene.addChild(cameraObj);
```

#### 可运行示例（FlyCameraController）

**WASD** 移动，**Q/E** 升降，**Shift** 加速，**F** 重置；先点击画布再拖拽转向。右侧调 `moveSpeed` / `shiftMoveScale`。

<div class="rings-quick-demo" style="margin: 1em 0; border: 1px solid #e2e8f0; border-radius: 8px; overflow: hidden;">
  <input type="radio" name="rings-fly-cam-demo-tab" id="rings-fly-cam-tab-preview" checked style="position: absolute; opacity: 0; pointer-events: none;">
  <input type="radio" name="rings-fly-cam-demo-tab" id="rings-fly-cam-tab-code" style="position: absolute; opacity: 0; pointer-events: none;">
  <style>
    .rings-quick-demo .rings-tab-bar { display: flex; border-bottom: 1px solid #e2e8f0; background: #f8fafc; }
    .rings-quick-demo .rings-tab-bar label { flex: 1; padding: 10px 16px; cursor: pointer; font-size: 14px; color: #64748b; text-align: center; margin: 0; }
    .rings-quick-demo #rings-fly-cam-tab-preview:checked ~ .rings-tab-bar label[for="rings-fly-cam-tab-preview"],
    .rings-quick-demo #rings-fly-cam-tab-code:checked ~ .rings-tab-bar label[for="rings-fly-cam-tab-code"] { font-weight: 600; color: #0f172a; }
    .rings-quick-demo .rings-demo-preview,
    .rings-quick-demo .rings-demo-code { display: none; height: 420px; }
    .rings-quick-demo #rings-fly-cam-tab-preview:checked ~ .rings-demo-preview,
    .rings-quick-demo #rings-fly-cam-tab-code:checked ~ .rings-demo-code { display: block; }
  </style>
  <div class="rings-tab-bar">
    <label for="rings-fly-cam-tab-preview">示例</label>
    <label for="rings-fly-cam-tab-code">代码</label>
  </div>
  <div class="rings-demo-preview" style="background: #fff;">
    <iframe src="examples/fly-camera-controller-demo.html" title="Rings FlyCamera 演示" style="width: 100%; height: 100%; border: none; min-height: 420px;"></iframe>
  </div>
  <div class="rings-demo-code" style="overflow: auto; background: #1e293b;">
    <pre style="margin: 0; padding: 16px; font-size: 12px; line-height: 1.5; color: #e2e8f0; white-space: pre;"><code>&lt;!DOCTYPE html&gt;
&lt;html lang="zh-CN"&gt;
&lt;head&gt;
  &lt;meta charset="UTF-8" /&gt;
  &lt;meta name="viewport" content="width=device-width, initial-scale=1.0" /&gt;
  &lt;title&gt;Rings — FlyCameraController 演示（可运行示例）&lt;/title&gt;
  &lt;style&gt;
    html, body { margin: 0; height: 100%; width: 100%; overflow: hidden; }
    body { position: relative; font-family: system-ui, "Segoe UI", sans-serif; }
    #canvas { display: block; width: 100%; height: 100%; }
    .hud {
      position: absolute; left: 8px; top: 8px; right: 220px; pointer-events: none;
      font-size: 11px; line-height: 1.45; color: #e6edf3; text-shadow: 0 1px 3px #000;
      font-family: ui-monospace, monospace;
    }
    .controls {
      position: absolute; top: 0; right: 0; bottom: 0; width: 200px; padding: 10px;
      overflow: auto; background: rgba(15, 20, 25, 0.92); color: #e6edf3;
      font-size: 0.78rem; border-left: 1px solid #2d3a4d;
    }
    .controls h3 { margin: 0 0 8px; font-size: 0.8rem; color: #8b9cb3; font-weight: 600; }
    .row { margin-bottom: 10px; }
    .row label { display: block; margin-bottom: 3px; color: #8b9cb3; }
    .row input[type="range"] { width: 100%; }
    .row .val { float: right; color: #58a6ff; }
    @media (max-width: 520px) {
      .hud { right: 8px; }
      .controls { width: 100%; height: auto; max-height: 45%; top: auto; left: 0; border-left: none; border-top: 1px solid #2d3a4d; }
    }
  &lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
  &lt;canvas id="canvas" style="width: 100%; height: 100%;"&gt;&lt;/canvas&gt;
  &lt;div class="hud" id="hud"&gt;&lt;/div&gt;
  &lt;aside class="controls"&gt;
    &lt;h3&gt;FlyCameraController&lt;/h3&gt;
    &lt;div class="row"&gt;
      &lt;label&gt;moveSpeed &lt;span class="val" id="speed-val"&gt;5&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="speed" min="1" max="20" value="5" step="0.5" /&gt;
    &lt;/div&gt;
    &lt;div class="row"&gt;
      &lt;label&gt;shiftMoveScale &lt;span class="val" id="shift-val"&gt;20&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="shift" min="5" max="40" value="20" step="1" /&gt;
    &lt;/div&gt;
    &lt;p style="margin:8px 0 0;color:#8b9cb3;line-height:1.45"&gt;
      先&lt;strong&gt;点击画布&lt;/strong&gt;，再拖拽转向。WASD 平移，Q/E 升降，&lt;strong&gt;Shift&lt;/strong&gt; 加速，&lt;strong&gt;F&lt;/strong&gt; 重置朝向。
    &lt;/p&gt;
  &lt;/aside&gt;

  &lt;script type="module"&gt;
    import {
      Engine3D,
      Scene3D,
      Object3D,
      Camera3D,
      View3D,
      Vector3,
      LitMaterial,
      BoxGeometry,
      SphereGeometry,
      PlaneGeometry,
      MeshRenderer,
      AtmosphericComponent,
      DirectLight,
      FlyCameraController,
      Color,
    } from 'https://unpkg.com/@rings-webgpu/core@1.0.52/dist/rings.es.js';

    function fmt(v) {
      return `${v.x.toFixed(2)}, ${v.y.toFixed(2)}, ${v.z.toFixed(2)}`;
    }

    async function main() {
      const canvas = document.getElementById('canvas');
      const hud = document.getElementById('hud');
      if (!canvas) throw new Error('缺少 #canvas');

      try {
        await Engine3D.init({ canvasConfig: { canvas } });
      } catch (e) {
        document.body.innerHTML =
          '&lt;p style="padding:1rem;font-family:sans-serif"&gt;WebGPU 初始化失败，请使用 Chrome/Edge 最新版并在 localhost 或 HTTPS 下打开。&lt;/p&gt;';
        console.error(e);
        return;
      }

      const scene = new Scene3D();
      const sky = scene.addComponent(AtmosphericComponent);
      sky.sunY = 0.6;
      sky.sunZ = 0.8;

      const cameraObj = new Object3D();
      const camera = cameraObj.addComponent(Camera3D);
      camera.perspective(60, canvas.clientWidth / Math.max(canvas.clientHeight, 1), 0.1, 1000);

      const fly = cameraObj.addComponent(FlyCameraController);
      fly.setCamera(new Vector3(0, 5, 12), new Vector3(0, 0, 0));
      fly.moveSpeed = 5;
      fly.config.shiftMoveScale = 20;
      scene.addChild(cameraObj);

      const lightObj = new Object3D();
      const light = lightObj.addComponent(DirectLight);
      light.lightColor = new Color(1, 1, 1, 1);
      light.intensity = 1.2;
      light.castShadow = true;
      scene.addChild(lightObj);

      const ground = new Object3D();
      const groundR = ground.addComponent(MeshRenderer);
      groundR.geometry = new PlaneGeometry(10, 10);
      groundR.material = new LitMaterial();
      groundR.material.baseColor = new Color(0.3, 0.3, 0.3, 1);
      groundR.receiveShadow = true;
      scene.addChild(ground);

      const box = new Object3D();
      const boxR = box.addComponent(MeshRenderer);
      boxR.geometry = new BoxGeometry(1, 1, 1);
      boxR.material = new LitMaterial();
      boxR.material.baseColor = new Color(0.8, 0.2, 0.2, 1);
      boxR.castShadow = true;
      box.y = 0.5;
      scene.addChild(box);

      const sphere = new Object3D();
      const sphereR = sphere.addComponent(MeshRenderer);
      sphereR.geometry = new SphereGeometry(0.5, 32, 32);
      sphereR.material = new LitMaterial();
      sphereR.material.baseColor = new Color(0.2, 0.8, 0.2, 1);
      sphereR.castShadow = true;
      sphere.x = 2;
      sphere.y = 0.5;
      scene.addChild(sphere);

      const view = new View3D();
      view.scene = scene;
      view.camera = camera;
      Engine3D.startRenderView(view);

      const speedEl = document.getElementById('speed');
      const speedVal = document.getElementById('speed-val');
      const shiftEl = document.getElementById('shift');
      const shiftVal = document.getElementById('shift-val');

      function bindSliders() {
        fly.moveSpeed = Number(speedEl.value);
        fly.config.shiftMoveScale = Number(shiftEl.value);
        speedVal.textContent = String(fly.moveSpeed);
        shiftVal.textContent = String(fly.config.shiftMoveScale);
      }
      speedEl.addEventListener('input', bindSliders);
      shiftEl.addEventListener('input', bindSliders);
      bindSliders();

      window.addEventListener('resize', () =&gt; {
        camera.perspective(
          60,
          canvas.clientWidth / Math.max(canvas.clientHeight, 1),
          0.1,
          1000
        );
      });

      function tick() {
        const lp = cameraObj.transform.localPosition;
        hud.innerHTML =
          `&lt;div&gt;&lt;b&gt;相机位置&lt;/b&gt; localPosition ${fmt(lp)}&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;setCamera&lt;/b&gt; targetPos ${fmt(fly.targetPos)} · lookAtPos ${fmt(fly.lookAtPos)}（移动中不随帧更新）&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;moveSpeed&lt;/b&gt; ${fly.moveSpeed.toFixed(1)} · &lt;b&gt;shiftMoveScale&lt;/b&gt; ${fly.config.shiftMoveScale}&lt;/div&gt;`;
        requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }

    main();
  &lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
  </div>
</div>

---

#### 悬浮相机控制器 (HoverCameraController)

实现围绕目标点的轨道控制，支持鼠标拖拽旋转和滚轮缩放。

**属性说明**

| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| `distance` | number | 10 | 与目标的距离 |
| `minDistance` | number | 1 | 最小距离限制 |
| `maxDistance` | number | 100 | 最大距离限制 |

**使用示例**

```typescript
import { Object3D, Scene3D, Camera3D, HoverCameraController, Vector3 } from '@rings/core';

// 创建场景
const scene = new Scene3D();

// 创建相机对象
const cameraObj = new Object3D();
const camera = cameraObj.addComponent(Camera3D);
const controller = cameraObj.addComponent(HoverCameraController);

// 设置相机参数
camera.perspective(60, window.innerWidth / window.innerHeight, 0.1, 1000);
controller.distance = 15;
controller.minDistance = 2;
controller.maxDistance = 50;

// 设置初始位置
cameraObj.transform.position = new Vector3(0, 10, 15);
scene.addChild(cameraObj);
```

#### 可运行示例（HoverCameraController）

**左键**旋转、**右键**平移、**滚轮**缩放；右侧调 `minDistance` / `maxDistance`。

<div class="rings-quick-demo" style="margin: 1em 0; border: 1px solid #e2e8f0; border-radius: 8px; overflow: hidden;">
  <input type="radio" name="rings-hover-cam-demo-tab" id="rings-hover-cam-tab-preview" checked style="position: absolute; opacity: 0; pointer-events: none;">
  <input type="radio" name="rings-hover-cam-demo-tab" id="rings-hover-cam-tab-code" style="position: absolute; opacity: 0; pointer-events: none;">
  <style>
    .rings-quick-demo .rings-tab-bar { display: flex; border-bottom: 1px solid #e2e8f0; background: #f8fafc; }
    .rings-quick-demo .rings-tab-bar label { flex: 1; padding: 10px 16px; cursor: pointer; font-size: 14px; color: #64748b; text-align: center; margin: 0; }
    .rings-quick-demo #rings-hover-cam-tab-preview:checked ~ .rings-tab-bar label[for="rings-hover-cam-tab-preview"],
    .rings-quick-demo #rings-hover-cam-tab-code:checked ~ .rings-tab-bar label[for="rings-hover-cam-tab-code"] { font-weight: 600; color: #0f172a; }
    .rings-quick-demo .rings-demo-preview,
    .rings-quick-demo .rings-demo-code { display: none; height: 420px; }
    .rings-quick-demo #rings-hover-cam-tab-preview:checked ~ .rings-demo-preview,
    .rings-quick-demo #rings-hover-cam-tab-code:checked ~ .rings-demo-code { display: block; }
  </style>
  <div class="rings-tab-bar">
    <label for="rings-hover-cam-tab-preview">示例</label>
    <label for="rings-hover-cam-tab-code">代码</label>
  </div>
  <div class="rings-demo-preview" style="background: #fff;">
    <iframe src="examples/hover-camera-controller-demo.html" title="Rings HoverCamera 演示" style="width: 100%; height: 100%; border: none; min-height: 420px;"></iframe>
  </div>
  <div class="rings-demo-code" style="overflow: auto; background: #1e293b;">
    <pre style="margin: 0; padding: 16px; font-size: 12px; line-height: 1.5; color: #e2e8f0; white-space: pre;"><code>&lt;!DOCTYPE html&gt;
&lt;html lang="zh-CN"&gt;
&lt;head&gt;
  &lt;meta charset="UTF-8" /&gt;
  &lt;meta name="viewport" content="width=device-width, initial-scale=1.0" /&gt;
  &lt;title&gt;Rings — HoverCameraController 演示（可运行示例）&lt;/title&gt;
  &lt;style&gt;
    html, body { margin: 0; height: 100%; width: 100%; overflow: hidden; }
    body { position: relative; font-family: system-ui, "Segoe UI", sans-serif; }
    #canvas { display: block; width: 100%; height: 100%; }
    .hud {
      position: absolute; left: 8px; top: 8px; right: 220px; pointer-events: none;
      font-size: 11px; line-height: 1.45; color: #e6edf3; text-shadow: 0 1px 3px #000;
      font-family: ui-monospace, monospace;
    }
    .controls {
      position: absolute; top: 0; right: 0; bottom: 0; width: 200px; padding: 10px;
      overflow: auto; background: rgba(15, 20, 25, 0.92); color: #e6edf3;
      font-size: 0.78rem; border-left: 1px solid #2d3a4d;
    }
    .controls h3 { margin: 0 0 8px; font-size: 0.8rem; color: #8b9cb3; font-weight: 600; }
    .row { margin-bottom: 10px; }
    .row label { display: block; margin-bottom: 3px; color: #8b9cb3; }
    .row input[type="range"] { width: 100%; }
    .row .val { float: right; color: #58a6ff; }
    @media (max-width: 520px) {
      .hud { right: 8px; }
      .controls { width: 100%; height: auto; max-height: 45%; top: auto; left: 0; border-left: none; border-top: 1px solid #2d3a4d; }
    }
  &lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
  &lt;canvas id="canvas" style="width: 100%; height: 100%;"&gt;&lt;/canvas&gt;
  &lt;div class="hud" id="hud"&gt;&lt;/div&gt;
  &lt;aside class="controls"&gt;
    &lt;h3&gt;HoverCameraController&lt;/h3&gt;
    &lt;div class="row"&gt;
      &lt;label&gt;minDistance &lt;span class="val" id="min-val"&gt;2&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="min" min="1" max="50" value="20" step="1" /&gt;
    &lt;/div&gt;
    &lt;div class="row"&gt;
      &lt;label&gt;maxDistance &lt;span class="val" id="max-val"&gt;50&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="max" min="30" max="500" value="50" step="5" /&gt;
    &lt;/div&gt;
    &lt;p style="margin:8px 0 0;color:#8b9cb3;line-height:1.45"&gt;
      &lt;strong&gt;左键&lt;/strong&gt;拖拽旋转视角，&lt;strong&gt;右键&lt;/strong&gt;平移，&lt;strong&gt;滚轮&lt;/strong&gt;缩放距离。初始 &lt;code&gt;setCamera(45, -30, 10)&lt;/code&gt;。
    &lt;/p&gt;
  &lt;/aside&gt;

  &lt;script type="module"&gt;
    import {
      Engine3D,
      Scene3D,
      Object3D,
      Camera3D,
      View3D,
      Vector3,
      LitMaterial,
      BoxGeometry,
      SphereGeometry,
      PlaneGeometry,
      MeshRenderer,
      AtmosphericComponent,
      DirectLight,
      HoverCameraController,
      Color,
    } from 'https://unpkg.com/@rings-webgpu/core@1.0.52/dist/rings.es.js';

    function fmt(v) {
      return `${v.x.toFixed(2)}, ${v.y.toFixed(2)}, ${v.z.toFixed(2)}`;
    }

    async function main() {
      const canvas = document.getElementById('canvas');
      const hud = document.getElementById('hud');
      if (!canvas) throw new Error('缺少 #canvas');

      try {
        await Engine3D.init({ canvasConfig: { canvas } });
      } catch (e) {
        document.body.innerHTML =
          '&lt;p style="padding:1rem;font-family:sans-serif"&gt;WebGPU 初始化失败，请使用 Chrome/Edge 最新版并在 localhost 或 HTTPS 下打开。&lt;/p&gt;';
        console.error(e);
        return;
      }

      const scene = new Scene3D();
      const sky = scene.addComponent(AtmosphericComponent);
      sky.sunY = 0.6;
      sky.sunZ = 0.8;

      const cameraObj = new Object3D();
      const camera = cameraObj.addComponent(Camera3D);
      camera.perspective(60, canvas.clientWidth / Math.max(canvas.clientHeight, 1), 0.1, 1000);

      const hover = cameraObj.addComponent(HoverCameraController);
      hover.setCamera(45, -30, 10);
      hover.minDistance = 2;
      hover.maxDistance = 50;
      scene.addChild(cameraObj);

      const lightObj = new Object3D();
      const light = lightObj.addComponent(DirectLight);
      light.lightColor = new Color(1, 1, 1, 1);
      light.intensity = 1.2;
      light.castShadow = true;
      scene.addChild(lightObj);

      const ground = new Object3D();
      const groundR = ground.addComponent(MeshRenderer);
      groundR.geometry = new PlaneGeometry(10, 10);
      groundR.material = new LitMaterial();
      groundR.material.baseColor = new Color(0.3, 0.3, 0.3, 1);
      groundR.receiveShadow = true;
      scene.addChild(ground);

      const box = new Object3D();
      const boxR = box.addComponent(MeshRenderer);
      boxR.geometry = new BoxGeometry(1, 1, 1);
      boxR.material = new LitMaterial();
      boxR.material.baseColor = new Color(0.8, 0.2, 0.2, 1);
      boxR.castShadow = true;
      box.y = 0.5;
      scene.addChild(box);

      const sphere = new Object3D();
      const sphereR = sphere.addComponent(MeshRenderer);
      sphereR.geometry = new SphereGeometry(0.5, 32, 32);
      sphereR.material = new LitMaterial();
      sphereR.material.baseColor = new Color(0.2, 0.8, 0.2, 1);
      sphereR.castShadow = true;
      sphere.x = 2;
      sphere.y = 0.5;
      scene.addChild(sphere);

      const view = new View3D();
      view.scene = scene;
      view.camera = camera;
      Engine3D.startRenderView(view);

      const minEl = document.getElementById('min');
      const minVal = document.getElementById('min-val');
      const maxEl = document.getElementById('max');
      const maxVal = document.getElementById('max-val');

      function bindSliders() {
        hover.minDistance = Number(minEl.value) / 10;
        hover.maxDistance = Number(maxEl.value);
        minVal.textContent = hover.minDistance.toFixed(1);
        maxVal.textContent = String(hover.maxDistance);
      }
      minEl.addEventListener('input', bindSliders);
      maxEl.addEventListener('input', bindSliders);
      bindSliders();

      window.addEventListener('resize', () =&gt; {
        camera.perspective(
          60,
          canvas.clientWidth / Math.max(canvas.clientHeight, 1),
          0.1,
          1000
        );
      });

      function tick() {
        hud.innerHTML =
          `&lt;div&gt;&lt;b&gt;distance&lt;/b&gt; ${hover.distance.toFixed(2)} · &lt;b&gt;roll&lt;/b&gt; ${hover.roll.toFixed(1)}° · &lt;b&gt;pitch&lt;/b&gt; ${hover.pitch.toFixed(1)}°&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;target&lt;/b&gt; ${fmt(hover.target)}&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;minDistance&lt;/b&gt; ${hover.minDistance.toFixed(2)} · &lt;b&gt;maxDistance&lt;/b&gt; ${hover.maxDistance}&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;smooth&lt;/b&gt; ${hover.smooth}&lt;/div&gt;`;
        requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }

    main();
  &lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
  </div>
</div>

---

#### 轨道相机控制器 (OrbitController)

实现围绕目标点的轨道控制，支持鼠标拖拽旋转、缩放和平移。

**属性说明**

| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| `target` | Vector3 | (0,0,0) | 轨道中心点 |
| `distance` | number | 10 | 与目标的距离 |
| `autoRotate` | boolean | false | 是否自动旋转 |

**使用示例**

```typescript
import { Object3D, Scene3D, Camera3D, OrbitController, Vector3 } from '@rings/core';

// 创建场景
const scene = new Scene3D();

// 创建相机对象
const cameraObj = new Object3D();
const camera = cameraObj.addComponent(Camera3D);
const controller = cameraObj.addComponent(OrbitController);

// 设置相机参数
camera.perspective(60, window.innerWidth / window.innerHeight, 0.1, 1000);
controller.target = new Vector3(0, 0, 0);
controller.distance = 20;
controller.autoRotate = true;

// 设置初始位置
cameraObj.transform.position = new Vector3(0, 10, 20);
scene.addChild(cameraObj);
```

#### 可运行示例（OrbitController）

**左键**旋转、**右键**平移、**滚轮**缩放；可勾选 **autoRotate**。

<div class="rings-quick-demo" style="margin: 1em 0; border: 1px solid #e2e8f0; border-radius: 8px; overflow: hidden;">
  <input type="radio" name="rings-orbit-cam-demo-tab" id="rings-orbit-cam-tab-preview" checked style="position: absolute; opacity: 0; pointer-events: none;">
  <input type="radio" name="rings-orbit-cam-demo-tab" id="rings-orbit-cam-tab-code" style="position: absolute; opacity: 0; pointer-events: none;">
  <style>
    .rings-quick-demo .rings-tab-bar { display: flex; border-bottom: 1px solid #e2e8f0; background: #f8fafc; }
    .rings-quick-demo .rings-tab-bar label { flex: 1; padding: 10px 16px; cursor: pointer; font-size: 14px; color: #64748b; text-align: center; margin: 0; }
    .rings-quick-demo #rings-orbit-cam-tab-preview:checked ~ .rings-tab-bar label[for="rings-orbit-cam-tab-preview"],
    .rings-quick-demo #rings-orbit-cam-tab-code:checked ~ .rings-tab-bar label[for="rings-orbit-cam-tab-code"] { font-weight: 600; color: #0f172a; }
    .rings-quick-demo .rings-demo-preview,
    .rings-quick-demo .rings-demo-code { display: none; height: 420px; }
    .rings-quick-demo #rings-orbit-cam-tab-preview:checked ~ .rings-demo-preview,
    .rings-quick-demo #rings-orbit-cam-tab-code:checked ~ .rings-demo-code { display: block; }
  </style>
  <div class="rings-tab-bar">
    <label for="rings-orbit-cam-tab-preview">示例</label>
    <label for="rings-orbit-cam-tab-code">代码</label>
  </div>
  <div class="rings-demo-preview" style="background: #fff;">
    <iframe src="examples/orbit-controller-demo.html" title="Rings OrbitController 演示" style="width: 100%; height: 100%; border: none; min-height: 420px;"></iframe>
  </div>
  <div class="rings-demo-code" style="overflow: auto; background: #1e293b;">
    <pre style="margin: 0; padding: 16px; font-size: 12px; line-height: 1.5; color: #e2e8f0; white-space: pre;"><code>&lt;!DOCTYPE html&gt;
&lt;html lang="zh-CN"&gt;
&lt;head&gt;
  &lt;meta charset="UTF-8" /&gt;
  &lt;meta name="viewport" content="width=device-width, initial-scale=1.0" /&gt;
  &lt;title&gt;Rings — OrbitController 演示（可运行示例）&lt;/title&gt;
  &lt;style&gt;
    html, body { margin: 0; height: 100%; width: 100%; overflow: hidden; }
    body { position: relative; font-family: system-ui, "Segoe UI", sans-serif; }
    #canvas { display: block; width: 100%; height: 100%; }
    .hud {
      position: absolute; left: 8px; top: 8px; right: 220px; pointer-events: none;
      font-size: 11px; line-height: 1.45; color: #e6edf3; text-shadow: 0 1px 3px #000;
      font-family: ui-monospace, monospace;
    }
    .controls {
      position: absolute; top: 0; right: 0; bottom: 0; width: 200px; padding: 10px;
      overflow: auto; background: rgba(15, 20, 25, 0.92); color: #e6edf3;
      font-size: 0.78rem; border-left: 1px solid #2d3a4d;
    }
    .controls h3 { margin: 0 0 8px; font-size: 0.8rem; color: #8b9cb3; font-weight: 600; }
    .row { margin-bottom: 10px; }
    .row label { display: block; margin-bottom: 3px; color: #8b9cb3; }
    .row input[type="range"] { width: 100%; }
    .row .val { float: right; color: #58a6ff; }
    .row label.chk { display: flex; align-items: center; gap: 8px; cursor: pointer; color: #e6edf3; }
    @media (max-width: 520px) {
      .hud { right: 8px; }
      .controls { width: 100%; height: auto; max-height: 45%; top: auto; left: 0; border-left: none; border-top: 1px solid #2d3a4d; }
    }
  &lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
  &lt;canvas id="canvas" style="width: 100%; height: 100%;"&gt;&lt;/canvas&gt;
  &lt;div class="hud" id="hud"&gt;&lt;/div&gt;
  &lt;aside class="controls"&gt;
    &lt;h3&gt;OrbitController&lt;/h3&gt;
    &lt;div class="row"&gt;
      &lt;label&gt;minDistance &lt;span class="val" id="min-val"&gt;2.0&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="min" min="10" max="80" value="20" step="1" /&gt;
    &lt;/div&gt;
    &lt;div class="row"&gt;
      &lt;label&gt;maxDistance &lt;span class="val" id="max-val"&gt;80&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="max" min="40" max="200" value="80" step="5" /&gt;
    &lt;/div&gt;
    &lt;div class="row"&gt;
      &lt;label class="chk"&gt;&lt;input type="checkbox" id="auto" /&gt; autoRotate&lt;/label&gt;
    &lt;/div&gt;
    &lt;div class="row"&gt;
      &lt;label&gt;autoRotateSpeed &lt;span class="val" id="speed-val"&gt;0.10&lt;/span&gt;&lt;/label&gt;
      &lt;input type="range" id="speed" min="2" max="30" value="10" step="1" /&gt;
    &lt;/div&gt;
    &lt;p style="margin:8px 0 0;color:#8b9cb3;line-height:1.45"&gt;
      &lt;strong&gt;左键&lt;/strong&gt;旋转轨道，&lt;strong&gt;右键&lt;/strong&gt;平移目标，&lt;strong&gt;滚轮&lt;/strong&gt;缩放。距离见 &lt;code&gt;spherical.radius&lt;/code&gt;。初始相机 &lt;code&gt;(0,10,20)&lt;/code&gt; 相对 &lt;code&gt;target (0,0,0)&lt;/code&gt;。
    &lt;/p&gt;
  &lt;/aside&gt;

  &lt;script type="module"&gt;
    import {
      Engine3D,
      Scene3D,
      Object3D,
      Camera3D,
      View3D,
      Vector3,
      LitMaterial,
      BoxGeometry,
      SphereGeometry,
      PlaneGeometry,
      MeshRenderer,
      AtmosphericComponent,
      DirectLight,
      OrbitController,
      Color,
    } from 'https://unpkg.com/@rings-webgpu/core@1.0.52/dist/rings.es.js';

    function fmt(v) {
      return `${v.x.toFixed(2)}, ${v.y.toFixed(2)}, ${v.z.toFixed(2)}`;
    }

    async function main() {
      const canvas = document.getElementById('canvas');
      const hud = document.getElementById('hud');
      if (!canvas) throw new Error('缺少 #canvas');

      try {
        await Engine3D.init({ canvasConfig: { canvas } });
      } catch (e) {
        document.body.innerHTML =
          '&lt;p style="padding:1rem;font-family:sans-serif"&gt;WebGPU 初始化失败，请使用 Chrome/Edge 最新版并在 localhost 或 HTTPS 下打开。&lt;/p&gt;';
        console.error(e);
        return;
      }

      const scene = new Scene3D();
      const sky = scene.addComponent(AtmosphericComponent);
      sky.sunY = 0.6;
      sky.sunZ = 0.8;

      const cameraObj = new Object3D();
      const camera = cameraObj.addComponent(Camera3D);
      camera.perspective(60, canvas.clientWidth / Math.max(canvas.clientHeight, 1), 0.1, 1000);

      cameraObj.transform.localPosition = new Vector3(0, 10, 20);

      const orbit = cameraObj.addComponent(OrbitController);
      orbit.target = new Vector3(0, 0, 0);
      orbit.minDistance = 2;
      orbit.maxDistance = 80;
      orbit.autoRotate = false;
      orbit.autoRotateSpeed = 0.1;

      scene.addChild(cameraObj);

      const lightObj = new Object3D();
      const light = lightObj.addComponent(DirectLight);
      light.lightColor = new Color(1, 1, 1, 1);
      light.intensity = 1.2;
      light.castShadow = true;
      scene.addChild(lightObj);

      const ground = new Object3D();
      const groundR = ground.addComponent(MeshRenderer);
      groundR.geometry = new PlaneGeometry(10, 10);
      groundR.material = new LitMaterial();
      groundR.material.baseColor = new Color(0.3, 0.3, 0.3, 1);
      groundR.receiveShadow = true;
      scene.addChild(ground);

      const box = new Object3D();
      const boxR = box.addComponent(MeshRenderer);
      boxR.geometry = new BoxGeometry(1, 1, 1);
      boxR.material = new LitMaterial();
      boxR.material.baseColor = new Color(0.8, 0.2, 0.2, 1);
      boxR.castShadow = true;
      box.y = 0.5;
      scene.addChild(box);

      const sphere = new Object3D();
      const sphereR = sphere.addComponent(MeshRenderer);
      sphereR.geometry = new SphereGeometry(0.5, 32, 32);
      sphereR.material = new LitMaterial();
      sphereR.material.baseColor = new Color(0.2, 0.8, 0.2, 1);
      sphereR.castShadow = true;
      sphere.x = 2;
      sphere.y = 0.5;
      scene.addChild(sphere);

      const view = new View3D();
      view.scene = scene;
      view.camera = camera;
      Engine3D.startRenderView(view);

      const minEl = document.getElementById('min');
      const minVal = document.getElementById('min-val');
      const maxEl = document.getElementById('max');
      const maxVal = document.getElementById('max-val');
      const autoEl = document.getElementById('auto');
      const speedEl = document.getElementById('speed');
      const speedVal = document.getElementById('speed-val');

      function bindSliders() {
        orbit.minDistance = Number(minEl.value) / 10;
        orbit.maxDistance = Number(maxEl.value);
        orbit.autoRotate = autoEl.checked;
        orbit.autoRotateSpeed = Number(speedEl.value) / 100;
        minVal.textContent = orbit.minDistance.toFixed(1);
        maxVal.textContent = String(orbit.maxDistance);
        speedVal.textContent = orbit.autoRotateSpeed.toFixed(2);
      }
      minEl.addEventListener('input', bindSliders);
      maxEl.addEventListener('input', bindSliders);
      autoEl.addEventListener('change', bindSliders);
      speedEl.addEventListener('input', bindSliders);
      bindSliders();

      window.addEventListener('resize', () =&gt; {
        camera.perspective(
          60,
          canvas.clientWidth / Math.max(canvas.clientHeight, 1),
          0.1,
          1000
        );
      });

      function tick() {
        const r = orbit.spherical.radius;
        hud.innerHTML =
          `&lt;div&gt;&lt;b&gt;target&lt;/b&gt; ${fmt(orbit.target)}&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;spherical.radius&lt;/b&gt; ${r.toFixed(2)}（滚轮缩放）&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;minDistance&lt;/b&gt; ${orbit.minDistance.toFixed(2)} · &lt;b&gt;maxDistance&lt;/b&gt; ${orbit.maxDistance}&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;autoRotate&lt;/b&gt; ${orbit.autoRotate} · &lt;b&gt;speed&lt;/b&gt; ${orbit.autoRotateSpeed.toFixed(2)}&lt;/div&gt;` +
          `&lt;div&gt;&lt;b&gt;相机 localPosition&lt;/b&gt; ${fmt(cameraObj.transform.localPosition)}&lt;/div&gt;`;
        requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }

    main();
  &lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
  </div>
</div>

---

#### 第一人称相机控制器 (FirstPersonCameraController)

与 **Camera3D** 挂在同一 **Object3D** 上，通过 **`focus`** 引用目标物体（如角色根节点）。每帧将相机置于焦点附近，拖拽指针修改旋转，滚轮调节 **`distance`**。

**使用示例**

```typescript
import { Object3D, Camera3D, FirstPersonCameraController } from '@rings/core';

const camObj = new Object3D();
const camera = camObj.addComponent(Camera3D);
camera.perspective(70, window.innerWidth / window.innerHeight, 0.1, 500);
const fp = camObj.addComponent(FirstPersonCameraController);
fp.focus = playerRoot;
fp.distance = 0.2;
```

#### 可运行示例（FirstPersonCameraController）

相机组件与控制器挂在同一物体上，**`focus`** 为角色根物体；点击画布后拖拽环顾，滚轮调 `distance`。

<div class="rings-quick-demo" style="margin: 1em 0; border: 1px solid #e2e8f0; border-radius: 8px; overflow: hidden;">
  <input type="radio" name="rings-firstperson-cam-demo-tab" id="rings-firstperson-cam-tab-preview" checked style="position: absolute; opacity: 0; pointer-events: none;">
  <input type="radio" name="rings-firstperson-cam-demo-tab" id="rings-firstperson-cam-tab-code" style="position: absolute; opacity: 0; pointer-events: none;">
  <style>
    .rings-quick-demo .rings-tab-bar { display: flex; border-bottom: 1px solid #e2e8f0; background: #f8fafc; }
    .rings-quick-demo .rings-tab-bar label { flex: 1; padding: 10px 16px; cursor: pointer; font-size: 14px; color: #64748b; text-align: center; margin: 0; }
    .rings-quick-demo #rings-firstperson-cam-tab-preview:checked ~ .rings-tab-bar label[for="rings-firstperson-cam-tab-preview"],
    .rings-quick-demo #rings-firstperson-cam-tab-code:checked ~ .rings-tab-bar label[for="rings-firstperson-cam-tab-code"] { font-weight: 600; color: #0f172a; }
    .rings-quick-demo .rings-demo-preview,
    .rings-quick-demo .rings-demo-code { display: none; height: 420px; }
    .rings-quick-demo #rings-firstperson-cam-tab-preview:checked ~ .rings-demo-preview,
    .rings-quick-demo #rings-firstperson-cam-tab-code:checked ~ .rings-demo-code { display: block; }
  </style>
  <div class="rings-tab-bar">
    <label for="rings-firstperson-cam-tab-preview">示例</label>
    <label for="rings-firstperson-cam-tab-code">代码</label>
  </div>
  <div class="rings-demo-preview" style="background: #fff;">
    <iframe src="examples/first-person-camera-demo.html" title="Rings 第一人称相机演示" style="width: 100%; height: 100%; border: none; min-height: 420px;"></iframe>
  </div>
  <div class="rings-demo-code" style="overflow: auto; background: #1e293b;">
    <pre style="margin: 0; padding: 16px; font-size: 12px; line-height: 1.5; color: #e2e8f0; white-space: pre;"><code>&lt;!DOCTYPE html&gt;
&lt;html lang="zh-CN"&gt;
&lt;head&gt;
  &lt;meta charset="UTF-8" /&gt;
  &lt;meta name="viewport" content="width=device-width, initial-scale=1.0" /&gt;
  &lt;title&gt;Rings — FirstPersonCameraController 演示&lt;/title&gt;
  &lt;style&gt;
    html, body { margin: 0; height: 100%; width: 100%; overflow: hidden; }
    body { position: relative; font-family: system-ui, "Segoe UI", sans-serif; }
    #canvas { display: block; width: 100%; height: 100%; }
    .hud {
      position: absolute; left: 8px; top: 8px; right: 8px; pointer-events: none;
      font-size: 11px; line-height: 1.45; color: #e6edf3; text-shadow: 0 1px 3px #000;
      font-family: ui-monospace, monospace;
    }
    .hint {
      position: absolute; bottom: 10px; left: 50%; transform: translateX(-50%);
      color: #8b9cb3; font-size: 12px; text-align: center; pointer-events: none;
    }
  &lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
  &lt;canvas id="canvas" style="width: 100%; height: 100%;"&gt;&lt;/canvas&gt;
  &lt;div class="hud" id="hud"&gt;&lt;/div&gt;
  &lt;p class="hint"&gt;点击画布后拖拽鼠标环顾；滚轮调节 distance（与焦点距离）&lt;/p&gt;
  &lt;script type="module"&gt;
    import {
      Engine3D,
      Scene3D,
      Object3D,
      Camera3D,
      View3D,
      Vector3,
      FirstPersonCameraController,
      LitMaterial,
      BoxGeometry,
      PlaneGeometry,
      MeshRenderer,
      AtmosphericComponent,
      DirectLight,
      Color,
    } from 'https://unpkg.com/@rings-webgpu/core@1.0.52/dist/rings.es.js';

    async function main() {
      const canvas = document.getElementById('canvas');
      const hud = document.getElementById('hud');
      if (!canvas) return;
      try {
        await Engine3D.init({ canvasConfig: { canvas } });
      } catch (e) {
        document.body.innerHTML =
          '&lt;p style="padding:1rem;font-family:sans-serif"&gt;WebGPU 初始化失败，请使用 Chrome/Edge 并在 localhost 或 HTTPS 下打开。&lt;/p&gt;';
        console.error(e);
        return;
      }

      const scene = new Scene3D();
      scene.addComponent(AtmosphericComponent);

      const sun = new Object3D();
      const dl = sun.addComponent(DirectLight);
      dl.lightColor = new Color(1, 0.98, 0.95, 1);
      dl.intensity = 1.2;
      sun.transform.rotationX = -45;
      sun.transform.rotationY = 30;
      scene.addChild(sun);

      const ground = new Object3D();
      const gmr = ground.addComponent(MeshRenderer);
      gmr.geometry = new PlaneGeometry(24, 24);
      gmr.material = new LitMaterial();
      gmr.material.baseColor = new Color(0.2, 0.22, 0.25, 1);
      ground.transform.rotationX = -90;
      scene.addChild(ground);

      const player = new Object3D();
      player.y = 0.9;
      const pmr = player.addComponent(MeshRenderer);
      pmr.geometry = new BoxGeometry(0.6, 1.2, 0.6);
      pmr.material = new LitMaterial();
      pmr.material.baseColor = new Color(0.3, 0.55, 0.9, 1);
      scene.addChild(player);

      const camObj = new Object3D();
      const camera = camObj.addComponent(Camera3D);
      camera.perspective(70, canvas.clientWidth / Math.max(canvas.clientHeight, 1), 0.1, 200);
      const fp = camObj.addComponent(FirstPersonCameraController);
      fp.focus = player;
      fp.distance = 0.15;
      scene.addChild(camObj);

      const view = new View3D();
      view.scene = scene;
      view.camera = camera;
      Engine3D.startRenderView(view);

      window.addEventListener('resize', () =&gt; {
        camera.perspective(70, canvas.clientWidth / Math.max(canvas.clientHeight, 1), 0.1, 200);
      });

      function tick() {
        hud.textContent = `FirstPerson · focus=玩家方块 · distance=${fp.distance.toFixed(2)}`;
        requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }
    main();
  &lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
  </div>
</div>

---

#### 第三人称相机控制器 (ThirdPersonCameraController)

相机位于 **`focus`** 世界位置沿相机朝向前向反方向 **`distance`** 处；拖拽环绕目标，滚轮调节距离。

**使用示例**

```typescript
import { Object3D, Camera3D, ThirdPersonCameraController } from '@rings/core';

const camObj = new Object3D();
const camera = camObj.addComponent(Camera3D);
camera.perspective(55, window.innerWidth / window.innerHeight, 0.1, 500);
const tp = camObj.addComponent(ThirdPersonCameraController);
tp.focus = targetRoot;
tp.distance = 6;
```

#### 可运行示例（ThirdPersonCameraController）

相机在目标后方 **distance** 处；拖拽环绕，滚轮缩放距离。

<div class="rings-quick-demo" style="margin: 1em 0; border: 1px solid #e2e8f0; border-radius: 8px; overflow: hidden;">
  <input type="radio" name="rings-thirdperson-cam-demo-tab" id="rings-thirdperson-cam-tab-preview" checked style="position: absolute; opacity: 0; pointer-events: none;">
  <input type="radio" name="rings-thirdperson-cam-demo-tab" id="rings-thirdperson-cam-tab-code" style="position: absolute; opacity: 0; pointer-events: none;">
  <style>
    .rings-quick-demo .rings-tab-bar { display: flex; border-bottom: 1px solid #e2e8f0; background: #f8fafc; }
    .rings-quick-demo .rings-tab-bar label { flex: 1; padding: 10px 16px; cursor: pointer; font-size: 14px; color: #64748b; text-align: center; margin: 0; }
    .rings-quick-demo #rings-thirdperson-cam-tab-preview:checked ~ .rings-tab-bar label[for="rings-thirdperson-cam-tab-preview"],
    .rings-quick-demo #rings-thirdperson-cam-tab-code:checked ~ .rings-tab-bar label[for="rings-thirdperson-cam-tab-code"] { font-weight: 600; color: #0f172a; }
    .rings-quick-demo .rings-demo-preview,
    .rings-quick-demo .rings-demo-code { display: none; height: 420px; }
    .rings-quick-demo #rings-thirdperson-cam-tab-preview:checked ~ .rings-demo-preview,
    .rings-quick-demo #rings-thirdperson-cam-tab-code:checked ~ .rings-demo-code { display: block; }
  </style>
  <div class="rings-tab-bar">
    <label for="rings-thirdperson-cam-tab-preview">示例</label>
    <label for="rings-thirdperson-cam-tab-code">代码</label>
  </div>
  <div class="rings-demo-preview" style="background: #fff;">
    <iframe src="examples/third-person-camera-demo.html" title="Rings 第三人称相机演示" style="width: 100%; height: 100%; border: none; min-height: 420px;"></iframe>
  </div>
  <div class="rings-demo-code" style="overflow: auto; background: #1e293b;">
    <pre style="margin: 0; padding: 16px; font-size: 12px; line-height: 1.5; color: #e2e8f0; white-space: pre;"><code>&lt;!DOCTYPE html&gt;
&lt;html lang="zh-CN"&gt;
&lt;head&gt;
  &lt;meta charset="UTF-8" /&gt;
  &lt;meta name="viewport" content="width=device-width, initial-scale=1.0" /&gt;
  &lt;title&gt;Rings — ThirdPersonCameraController 演示&lt;/title&gt;
  &lt;style&gt;
    html, body { margin: 0; height: 100%; width: 100%; overflow: hidden; }
    body { position: relative; font-family: system-ui, "Segoe UI", sans-serif; }
    #canvas { display: block; width: 100%; height: 100%; }
    .hud {
      position: absolute; left: 8px; top: 8px; right: 8px; pointer-events: none;
      font-size: 11px; line-height: 1.45; color: #e6edf3; text-shadow: 0 1px 3px #000;
      font-family: ui-monospace, monospace;
    }
    .hint {
      position: absolute; bottom: 10px; left: 50%; transform: translateX(-50%);
      color: #8b9cb3; font-size: 12px; text-align: center; pointer-events: none;
    }
  &lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
  &lt;canvas id="canvas" style="width: 100%; height: 100%;"&gt;&lt;/canvas&gt;
  &lt;div class="hud" id="hud"&gt;&lt;/div&gt;
  &lt;p class="hint"&gt;点击画布后拖拽环绕目标；滚轮拉近/拉远&lt;/p&gt;
  &lt;script type="module"&gt;
    import {
      Engine3D,
      Scene3D,
      Object3D,
      Camera3D,
      View3D,
      Vector3,
      ThirdPersonCameraController,
      LitMaterial,
      BoxGeometry,
      PlaneGeometry,
      MeshRenderer,
      AtmosphericComponent,
      DirectLight,
      Color,
    } from 'https://unpkg.com/@rings-webgpu/core@1.0.52/dist/rings.es.js';

    async function main() {
      const canvas = document.getElementById('canvas');
      const hud = document.getElementById('hud');
      if (!canvas) return;
      try {
        await Engine3D.init({ canvasConfig: { canvas } });
      } catch (e) {
        document.body.innerHTML =
          '&lt;p style="padding:1rem;font-family:sans-serif"&gt;WebGPU 初始化失败，请使用 Chrome/Edge 并在 localhost 或 HTTPS 下打开。&lt;/p&gt;';
        console.error(e);
        return;
      }

      const scene = new Scene3D();
      scene.addComponent(AtmosphericComponent);

      const sun = new Object3D();
      const dl = sun.addComponent(DirectLight);
      dl.lightColor = new Color(1, 0.98, 0.95, 1);
      dl.intensity = 1.2;
      sun.transform.rotationX = -45;
      sun.transform.rotationY = 30;
      scene.addChild(sun);

      const ground = new Object3D();
      const gmr = ground.addComponent(MeshRenderer);
      gmr.geometry = new PlaneGeometry(24, 24);
      gmr.material = new LitMaterial();
      gmr.material.baseColor = new Color(0.2, 0.22, 0.25, 1);
      ground.transform.rotationX = -90;
      scene.addChild(ground);

      const target = new Object3D();
      target.y = 0.9;
      const tmr = target.addComponent(MeshRenderer);
      tmr.geometry = new BoxGeometry(0.7, 1.4, 0.7);
      tmr.material = new LitMaterial();
      tmr.material.baseColor = new Color(0.85, 0.45, 0.2, 1);
      scene.addChild(target);

      const camObj = new Object3D();
      const camera = camObj.addComponent(Camera3D);
      camera.perspective(55, canvas.clientWidth / Math.max(canvas.clientHeight, 1), 0.1, 200);
      const tp = camObj.addComponent(ThirdPersonCameraController);
      tp.focus = target;
      tp.distance = 6;
      scene.addChild(camObj);

      const view = new View3D();
      view.scene = scene;
      view.camera = camera;
      Engine3D.startRenderView(view);

      window.addEventListener('resize', () =&gt; {
        camera.perspective(55, canvas.clientWidth / Math.max(canvas.clientHeight, 1), 0.1, 200);
      });

      function tick() {
        hud.textContent = `ThirdPerson · distance=${tp.distance.toFixed(2)}`;
        requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }
    main();
  &lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
  </div>
</div>

---

